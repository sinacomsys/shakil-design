import { TabsProps } from "../../tabs.type";
declare const CardNavList: ({
  items,
  onChange,
  onClose,
  activeTab,
}: Pick<
  TabsProps,
  "items" | "onChange" | "onClose" | "activeTab"
>) => import("react/jsx-runtime").JSX.Element;
export { CardNavList };
